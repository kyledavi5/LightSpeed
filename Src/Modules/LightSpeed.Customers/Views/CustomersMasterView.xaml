<UserControl x:Class="LightSpeed.Customers.Views.CustomersMasterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True" >
    <StackPanel>
        <TextBlock Text="Customers Master" FontSize="18" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,0,0,10" />
        <Button Content="New" Command="{Binding OpenCustomerDetailsDialogCommand}" CommandParameter="Create" HorizontalAlignment="Right" VerticalAlignment="Top" Width="100" Height="25" Margin="0,10,45,10" />
        <DataGrid AutoGenerateColumns="True" IsSynchronizedWithCurrentItem="True" IsReadOnly="True" 
                  ItemsSource="{Binding CustomerCollection}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}" 
                  Width="800" Height="400" Margin="0,5,0,0"  HorizontalAlignment="Center" VerticalAlignment="Top">
            <DataGrid.InputBindings>
                <MouseBinding Gesture="LeftDoubleClick"
                              Command="{Binding OpenCustomerDetailsDialogCommand}" CommandParameter="Update" />
            </DataGrid.InputBindings>
        </DataGrid>
    </StackPanel>
</UserControl>

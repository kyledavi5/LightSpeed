<UserControl x:Class="LightSpeed.ModuleA.Views.MasterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True" >
    <StackPanel>
        <TextBlock Text="Module A Master View" FontSize="18" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,5,0,10" />
        <Button Content="New" Command="{Binding ShowDialogCommand}" HorizontalAlignment="Right" VerticalAlignment="Top"  Margin="0,10,0,50" />
        <DataGrid AutoGenerateColumns="True" ItemsSource="{Binding ModuleAItems}" Width="800" Height="400" Margin="0,5,0,0"  HorizontalAlignment="Center" VerticalAlignment="Top" />
    </StackPanel>
</UserControl>
